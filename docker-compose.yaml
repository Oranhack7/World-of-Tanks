version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb://mongo:27017/world_of_tanks_db
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  app-tests: 
    build: 
      context: .
      dockerfile: Dockerfile.test
    environment:
      - TEST_MONGODB_URI=mongodb://mongo:27017/world_of_tanks_db
    depends_on:
      - mongo

volumes:
  mongo_data:
